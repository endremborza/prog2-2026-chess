Quick run

Use the project's virtualenv so the correct packages are available:

```
./run_kesonleadas.sh
```

Alternatively, open the project in VS Code and ensure the interpreter is set to `.venv/bin/python` (already configured in `.vscode/settings.json`), then click Run File.
